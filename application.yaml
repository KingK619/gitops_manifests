apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-gitops-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
      - name: fastapi-container
        image: southamerica-west1-docker.pkg.dev/project-f37a4860-d512-457a-b73/devsecops-docker-repo/fast-api-microservice:latest
        ports:
          - containerPort: 8000
